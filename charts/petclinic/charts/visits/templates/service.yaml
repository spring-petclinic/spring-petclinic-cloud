apiVersion: v1
kind: Service
metadata:
  name: {{ include "visits.fullname" . }}
  labels:
    {{- include "visits.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "visits.selectorLabels" . | nindent 4 }}
